const curveWords = ['ks', 'roc', 'gain', 'lift']
const loss = 'loss'
const stepwise = 'stepwise'

function exchange(list) {
  for (const namespace in list) {
    for (const item of list[namespace]) {
      
    }
  }
}